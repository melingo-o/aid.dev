rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This project uses Vercel Serverless + Firebase Admin SDK.
    // All client-side direct reads/writes are blocked.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
